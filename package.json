{
  "name": "blog-nous-my-id",
  "version": "1.0.0",
  "description": "Blog by NousVault",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve . -p 3000"
  },
  "dependencies": {
    "marked": "^12.0.0"
  }
}
